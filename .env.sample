# Public site URLs (used for local reference; notification emails read the same names from PavleTekBackend .env)
LandingPageURL=http://localhost:5173
AdminPageURL=http://localhost:5174

VITE_API_BASE_URL=http://localhost:3001/api
VITE_ALLOWED_HOSTS=localhost
# Vite dev server port (npm run dev). Default 5174 so it does not clash with PavletekLandingPage (5173).
VITE_DEV_PORT=5174